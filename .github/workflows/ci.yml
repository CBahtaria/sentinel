name: UEDF Sentinel CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Keep testing across these versions to ensure stability
        php-versions: ['8.1', '8.2', '8.3']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4 # Updated from v3
    
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php-versions }}
        extensions: pdo, pdo_mysql, json
        coverage: xdebug
    
    - name: Get composer cache directory
      id: composer-cache
      run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT
    
    - name: Cache dependencies
      uses: actions/cache@v4 # Updated from v3
      with:
        path: ${{ steps.composer-cache.outputs.dir }}
        key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
        restore-keys: ${{ runner.os }}-composer-
    
    - name: Install dependencies
      run: composer install --prefer-dist --no-progress
    
    - name: Run test suite
      run: composer test
    
    - name: Upload test results
      uses: actions/upload-artifact@v4 # Updated from v3 - Fixes the error
      if: always()
      with:
        # Note: v4 requires unique names if multiple jobs upload.
        # This adds the PHP version to the name to avoid conflicts in the matrix.
        name: test-results-php-${{ matrix.php-versions }}
        path: tests/_output/

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4 # Updated from v3
    
    - name: Deploy to production
      run: |
        echo "🚀 Tests passed! Ready for deployment"
        # Add your deployment commands here
